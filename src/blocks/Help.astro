---
import { Section, H2, H3, P, Image, Icon, Button } from "ui";
---

<!-- Help -->
<Section name="help">
  <div class="help__content group">
    <H2>Психологическая помощь <strong>родственникам</strong></H2>
    <p class="help__content-desc">
      Если в семье есть алкоголик, психологическое сопровождение необходимо
      родным и близким. Часто родственники не осознают, что своим поведением
      вносят негативный вклад в развитие алкогольной зависимости у члена семьи.
      Грамотный психолог на дому поможет выявить ошибки, даст полезные
      рекомендации.
    </p>
    <Button data-modal="form">Получить помощь</Button>
  </div>

  <div class="help__img">
    <Image src="help.png" alt="Психологическая помощь" />
  </div>
</Section>

<style lang="scss" is:global>
  @use "scss/helpers" as *;

  .help {
    position: relative;

    .container {
      position: relative;
    }

    &__content {
      position: relative;
      margin-left: rem(500);
      padding: rem(45) rem(120) rem(45) 0;
      gap: rem(35);

      @include media("xl") {
        margin-left: rem(400);
      }
      @include media("lg") {
        margin-left: 0;
        width: 100%;
      }
      @include media("sm") {
        padding: rem(25) rem(15) rem(30) 0;
        gap: rem(15);
      }

      &::before {
        content: "";
        position: absolute;
        top: 0;
        right: 0;
        z-index: -1;
        height: 100%;
        width: 100vw;
        background: #fff;
        box-shadow: 0 0 rem(125) 0 rgba(0, 0, 0, 0.15);
        border-radius: 0 rem(20) rem(20) 0;
      }

      .text-h2 {
        max-width: rem(800);
      }

      &-desc {
        margin-bottom: rem(10);
        max-width: rem(750);
        font-family: var(--font-secondary);

        @include media("sm") {
          font-size: rem(14);
        }
      }
    }

    &__img {
      position: absolute;
      top: 50%;
      left: var(--content-padding);
      transform: translateY(-50%);
      width: rem(455);
      pointer-events: none;

      @include media("xl") {
        width: rem(355);
      }
      @include media("lg") {
        display: none;
      }

      img {
        width: 100%;
      }
    }
  }
</style>
